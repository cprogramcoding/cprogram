#include<stdio.h>
void main()
{
	int a[]={2,4,5,6,78,1,8};
	int i,j,cnt=0;
	int len=sizeof(a)/sizeof(int);
	for(i=0;i<len;i++)
	{	
		for(j=0;j<len;j++)
		{
			if(i1=j&&a[i]=a[j])
			{
				cnt++;
				break;
			}
		}
	}
	if(c==0)
	printf("UNIQUE"):
	else
	printf("DUPLICATE");
	getch();
}

out put
UNIQUE
