#include<stdio.h>
#include<string.h>
int main()
{
	int i;
	char t[100];
	printf("enter strings");
	gets(t);
	len=strlen(t);
	for(i=0;i!='\0';i++)
	{
		if(i%2==0)
		{
			if(a[i]>64&&a[i]<94)
			a[i]+=32;
		}
		else
		{
			if(a[i]>96&&a[i]<123)
			a[i]-=32;
		}
	}
	printf("string changed is %s",t);
	return 0;
}
